name: Report

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed
jobs:
  report:
    name: 📜 Test Report
    runs-on: ubuntu-latest
    steps:
      - uses: dorny/test-reporter@v1
        with:
          artifact: test-results
          name: Tests
          path: "*.trx"
          reporter: dotnet-trx
